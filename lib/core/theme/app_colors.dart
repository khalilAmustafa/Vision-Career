import 'package:flutter/material.dart';

class AppColors {
  // Primary (brand color)
  static const primary = Color(0xFF7B61FF);

  // ================= DARK =================
  static const darkBackground = Color(0xFF0B0F1A);
  static const darkSurface = Color(0xFF121826);
  static const darkSurfaceSoft = Color(0xFF1A2233);

  static const darkTextPrimary = Colors.white;
  static const darkTextSecondary = Color(0xFF9AA4B2);

  // ================= LIGHT =================
  static const lightBackground = Color(0xFFF5F7FB);
  static const lightSurface = Colors.white;
  static const lightSurfaceSoft = Color(0xFFE5E9F2);

  static const lightTextPrimary = Color(0xFF0B0F1A);
  static const lightTextSecondary = Color(0xFF6B7280);

  // ================= STATUS =================
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
  static const warning = Color(0xFFF59E0B);

  // ================= DIVIDER =================
  static const dividerDark = Color(0xFF1F2937);
  static const dividerLight = Color(0xFFE5E7EB);
}